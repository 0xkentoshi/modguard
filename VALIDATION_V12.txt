ModGuard v1.2 Public GitHub-safe update — validation

Public build was tested with private_ops/ physically absent.
  255 passed
  three public regression runs passed

Compile check:
  python -m compileall -q app tests

Contains public agent improvements only:
  - automatic safety circuit / fail-closed SHADOW
  - configurable LIGHT reputation decay (default 6h)
  - compact Settings + Safety & tools split
  - Immunity List
  - Fake Admin / Fake Moderator protection

Does NOT contain:
  - private_ops/
  - superadmin renter control database
  - real .env
  - runtime databases
